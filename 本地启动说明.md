# AIedu 本地启动说明

项目运行时统一使用 FastAPI，Spring Boot 源码仅作为历史只读参考，不需要启动。

## 服务端口

- Vue 前端：`http://localhost:8080`
- FastAPI：`http://localhost:9091`
- FastAPI 文档：`http://localhost:9091/docs`
- MySQL：`localhost:3306`
- Redis：`localhost:6379`
- RocketMQ、MinIO、Qdrant：由根目录 `docker-compose.yml` 管理

所有浏览器业务请求统一使用 `/api/v1`，开发服务器将该前缀代理到 FastAPI。认证统一使用 FastAPI Bearer Token 和 HttpOnly Refresh Token Cookie。

## 推荐启动顺序

```powershell
# 1. 在项目根目录启动中间件
docker compose up -d

# 2. 执行数据库迁移并启动 FastAPI
.\scripts\migrate-fastapi.ps1
.\scripts\start-fastapi.ps1

# 3. 另开终端启动异步 Worker
.\scripts\start-worker.ps1

# 4. 另开终端启动 Vue
.\scripts\start-frontend.ps1
```

`start-fastapi.ps1` 默认采用稳定的单进程模式；仅在确实需要代码热重载时显式传入 `-Reload`。

## 容器化验收

```powershell
docker compose --profile container-app up -d --build
```

## 快速检查

```powershell
Invoke-RestMethod http://127.0.0.1:9091/health/ready
```

返回 `status: ready` 且 `database: ok` 表示 FastAPI 和数据库连接正常。前端构建使用：

```powershell
Set-Location 'Aiedu-student-study 前端'
npm run build
```

后端测试使用：

```powershell
Set-Location 'Aiedu-student-study FastAPI后端'
uv run pytest -q
```

## 开发约束

- 新增或修改业务接口只进入 `Aiedu-student-study FastAPI后端`。
- 前端统一通过 `src/utils/apiV1.js` 发起请求。
- 不再添加角色专用令牌头或第二套 Axios 实例。
- 数据库结构变更必须通过 Alembic。
- Spring Boot 目录不得作为运行依赖或新增功能目标。
