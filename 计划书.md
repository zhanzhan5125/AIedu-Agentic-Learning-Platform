# AIedu 技术栈迁移与业务完善计划书

> 实施决策（2026-09-21）：前端和运行环境已统一切换到 FastAPI `/api/v1`。Spring Boot 仅作为冻结的历史源码和只读行为参考，不再启动、不再作为运行依赖，也不再承载任何新增功能。

> 实施状态（2026-09-19）：阶段 1 和基础设施已完成；阶段 2 已建立目标模型、Alembic 基线与第一批幂等迁移；阶段 3 已完成新核心 API 的第一条可测试纵向链路；阶段 4 已接入并实测 Redis、RocketMQ Outbox、LangChain/LangGraph Worker；阶段 5 已开放 AI 问答和消息入口，未完成模块使用明确建设中页面。Docker 中间件和宿主机热重载链路已联调通过。旧 Spring Boot 仍作为业务回归基线，尚未执行正式数据迁移或生产切流。

本项目采用并行迁移策略：保留当前可运行的 Spring Boot 服务作为回归基线，在 `Aiedu-student-study FastAPI后端` 中建设 FastAPI 服务，验证功能与数据后再切换前端代理。

## 目标架构

- 前端保留 Vue 2.6.14，增加角色路由和 FastAPI v1 客户端。
- FastAPI 提供认证、课程、作业、提交、批阅、AI 任务、通知和文件接口。
- MySQL 保存业务真相；Redis 保存会话、撤销令牌、幂等锁和任务进度。
- RocketMQ 传递耗时 AI 任务；独立 Worker 使用 LangChain/LangGraph 执行任务。
- 旧数据通过 Alembic 和可重复的数据迁移程序导入新模型，切换前不修改现有业务表。

## 固定版本

Python 3.11.16、FastAPI 0.141.1、SQLAlchemy 2.0.54、Alembic 1.20.0、LangChain 1.4.1、LangGraph 1.2.11、RocketMQ Python Client 5.1.2、RocketMQ 5.3.2、Redis 7.4.11、MySQL 8.0.36、Node.js 22.23.2、Vue 2.6.14。

## 实施阶段

1. 建立当前接口、页面和数据库基线，备份现有数据库。
2. 建设 FastAPI 骨架、统一响应、认证、RBAC、日志和健康检查。
3. 建立目标数据模型和迁移脚本，校验用户、课程、作业、答案和成绩。
4. 迁移管理员、教师和学生的核心课程与作业流程。
5. 接入 Redis、RocketMQ、LangChain、LangGraph 和可恢复 AI 任务。
6. 依次开放已有 AI 页面、消息中心、课程资料、考试和个人中心。
7. 完成契约、集成、端到端、安全和迁移测试后切换流量；Spring Boot 保留一个发布周期作为只读回退。

## 核心约束

- 所有分页统一返回 `data.total/page/page_size/records`。
- 使用正确 HTTP 状态码，并保持 `{code,msg,data,request_id}` 响应格式。
- 身份从令牌取得，不能信任请求中的教师或学生账号。
- 作业发布、提交、批阅和消息消费必须幂等。
- 新密码使用 Argon2id；旧 MD5 密码在首次成功登录时升级。
- 密钥只能通过环境变量或密钥服务注入。
- 未完成入口显示明确的建设状态，不能继续使用无说明的禁用按钮。

## 验收

- 开发环境执行 `docker compose up -d` 只启动 MySQL、Redis 和 RocketMQ；FastAPI、Worker、Vue 在宿主机热重载运行。
- CI/整体验收执行 `docker compose --profile container-app up -d --build` 可启动完整容器栈。
- 三种角色能完成登录、课程、作业、提交和批阅主流程。
- 正常操作不存在缺失接口，所有资源均有角色和归属校验。
- 重复发布、重复提交、重复消息和 Worker 重启不会产生重复业务数据。
- 迁移前后核心表数量、抽样内容和成绩一致，且具有可执行回滚方案。

## 本次落地清单

| 项目 | 状态 | 说明 |
|---|---|---|
| FastAPI 分层、OpenAPI、请求 ID、统一异常 | 已完成 | 新后端目录独立运行于 9091 |
| 认证、RBAC、Argon2id、MD5 渐进升级 | 已完成 | Access/Refresh、Redis 会话与撤销 |
| 课程、作业、提交、人工批阅核心链路 | 已完成第一版 | 含发布/提交幂等、归属校验、未提交名单、成绩导出 |
| Redis、RocketMQ、Outbox、Worker | 已完成并通过本机联调 | Topic/消费组自动初始化；真实 Outbox 消息已完成发布、消费、LangGraph 执行和任务回写，重复幂等请求返回同一任务 |
| LangChain、LangGraph | 已完成骨架 | 无模型密钥时使用确定性替身，真实模型由环境变量启用 |
| 提示词、消息、文件 | 已完成第一版 | 文件限制格式、大小与所有权 |
| 旧数据迁移 | 样本双次演练通过 | 第二次新增数均为 0；项目样本数据报告 6 条孤儿记录，正式备份仍需双次演练 |
| 前端迁移 | 过渡态且已联调 | Node 22 构建通过，Vue `/api/v1` 代理到宿主机 FastAPI；其余旧页面仍需逐模块切换 |
| 云盘/资料/考试/个人中心 | 路线中 | 菜单已显式显示真实页面或“建设中”，未伪装为已完成 |
| 多智能体教学闭环 | 已完成第一版 | 知识点/证据/掌握度、Agent Run、智能出题草稿、个性化练习、批阅后画像刷新与学情接口已落地 |
| 课程资料 RAG | 已完成第一版 | MinIO/本地兼容存储、PDF/DOCX/PPTX 解析、Qdrant 索引、课程权限、预览下载与问答引用 |
| 防抄问答 | 已完成第一版 | 开放作业题干匹配后切换苏格拉底式提示，并保存策略、相似度和关联作业审计字段 |
| 课程消息 | 已完成第一版 | 教师公告、同班私聊、跨课程聚合、已读状态、计划截止通知和 WebSocket 增量推送 |
| 正式切流 | 未执行 | 当前无 Docker 环境，也未获生产备份/停写窗口，不应提前切换 |

## 当前已知风险

- 按要求保留并锁定 Vue 2.6.14 后，`npm audit` 报告 65 个旧依赖链漏洞（12 low、26 moderate、22 high、5 critical）。本轮不使用可能破坏 Vue 2 兼容性的 `npm audit fix --force`；公网发布前必须逐项审计，能无损升级的依赖先升级，其余风险纳入后续 Vue 3 专项迁移。
- Docker Desktop、中间件健康检查、RocketMQ Topic/消费组和宿主机 Worker 已实跑；RocketMQ Python SDK 在 Windows 上的硬编码日志路径已由适配层重定向到 D 盘。
- RocketMQ 5.3.2 首次建立 SimpleConsumer 时偶发一次服务端 Settings 同步空指针，Worker 会退避重试并随后稳定消费；升级 RocketMQ 前需保留此回归用例。
- 项目自带 `AIedu.sql` 本身缺少 `schedule_sheet` 记录，却包含引用这些记录的作业/选课数据，因此样本迁移报告 6 条孤儿记录；这是源数据问题，不应静默补造关系。
- 旧 `answer_sheet` 不含作业编号。同一题目复用于多份作业时迁移脚本会报告 `ambiguous_answers`；这类记录必须在切换前人工核对。
- 前端仓库原本已有大量未提交/未跟踪修改；本次没有重置或覆盖这些改动。旧后端仓库存在 Git 对象读取错误，正式建迁移分支前应先修复或重新克隆仓库。
