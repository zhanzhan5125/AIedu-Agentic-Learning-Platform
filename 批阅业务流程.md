# AIedu 作业批阅业务流程

## 一、统一原则

1. “是否提交”“由谁批阅”“是否需要教师确认”“成绩是否已发布”是四件不同的事，不能继续混在一个中文状态字段中。
2. AI 是教师的批阅助手，不是提交成立的前提。学生一旦正式提交，就计入“已交”。
3. 默认不让 AI 结果直接成为学生可见的最终成绩；AI 先给建议，教师确认或修改后才完成批阅。
4. 教师可以跳过 AI，直接人工批阅任何已提交作业。
5. 截止前学生再次保存或提交会使旧批阅结果失效，最终以最后一次提交内容和时间为准。

## 二、推荐状态流

```text
未开始 -> 草稿 -> 已提交 -> AI批阅中 -> 待教师确认 -> 已批阅 -> 已发回
                    |                         ^
                    +------ 教师直接批阅 -----+
```

- `not_started`：未作答，不计入已交。
- `draft`：保存过快照但未正式提交，不计入已交；截止时由系统把最新快照自动提交。
- `submitted`：已正式提交，计入已交，等待选择人工或 AI 辅助批阅。
- `ai_grading`：AI 任务执行中，仍计入已交。
- `needs_review`：AI 已给出分数和评语建议，等待教师确认；低置信度、格式校验失败等原因记录在 `review_reason`。
- `graded`：教师已确认最终分数，记录 `graded_at` 和 `grading_source`。
- `returned`：成绩与评语已向学生发布。

## 三、教师的正常操作

### AI 辅助模式（推荐默认）

1. 学生提交后显示为“待批阅”。
2. 到截止时间后由系统批量发起 AI 批阅，或由教师提前手动触发。
3. AI 完成后进入“AI 结果待教师确认”。高置信度结果可批量确认，低置信度结果必须逐份检查。
4. 教师可以修改每题分数、评语和总评，保存后才进入“已批阅”。
5. 教师发布成绩后进入“已发回”。

### 纯人工模式

教师从“待批阅”直接打开作业并评分，保存后进入“已批阅”。不要求先运行 AI。

## 四、统计口径

- 发送给：课程选课人数。
- 已交：`submitted / ai_grading / needs_review / graded / returned` 的总数。
- 未提交：未生成提交记录或状态为 `not_started / draft` 的学生。
- 待批阅：状态为 `submitted`。
- AI 批阅中：状态为 `ai_grading`。
- AI 结果待教师确认：状态为 `needs_review`。
- 已完成：状态为 `graded / returned`。

这些数量互斥且可以相加核对，不再用当前分页的记录数代替总数。

## 五、“标记错误批阅（智能）”的处理

该能力有价值，但不是主流程状态。调整为“反馈 AI 批阅问题”，并且只在当前答案确实存在 AI 建议时显示。教师修改 AI 分数或评语时，系统可自动保存修改前后差异；按钮只用于补充错误类型和原因，形成脱敏的改进样本。纯人工批阅时不显示，也不影响最终成绩状态。
